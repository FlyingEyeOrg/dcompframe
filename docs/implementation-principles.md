# 实现原则

## 1. 先测后码

所有新增功能先由单元测试定义行为，再实现最小通过代码。

## 2. 最小可用实现

优先交付阶段性可验证骨架，避免过度设计。

## 3. 资源与状态安全

- 使用值语义与明确状态转换。
- 避免隐藏副作用。
- 对关键动作设置前置条件（如提交门禁）。

## 4. Windows 约束优先

- 默认遵循 `WS_EX_NOREDIRECTIONBITMAP`。
- DPI 计算采用 96 作为基线。

## 5. 可测试性优先

- 模块接口小而清晰。
- 布局与事件逻辑以确定性行为为主，便于断言。
