# DCompFrame 完整产品开发 Todo

## 交付状态（2026-04-09）

- [x] P0-1 真实 DirectX 后端链路（D3D11 + DXGI + DComp）稳定化
- [x] P0-2 配置驱动渲染参数（JSON）
- [x] P0-3 窗口生命周期全链路
- [x] P0-4 UI 核心（脏标记/焦点/属性一致性）
- [x] P0-5 Grid/Stack 在 DPI 变化场景下基础一致性
- [x] P0-6 控件层基础闭环（含新增 TextBox/ListView/ScrollViewer/CheckBox/Slider）
- [x] P0-7 动画系统（时间线/缓动/并发）
- [x] P0-8 资源管理与设备恢复主流程
- [x] P0-9 单元与集成测试覆盖关键路径
- [x] P0-10 可长期运行 demo（关闭后退出）

## P0 核心可用（必须完成）

1. 完成真实 DirectX 后端链路（D3D11 + DXGI + DirectComposition）稳定化。
2. 接入 Direct2D + DirectWrite 到 D3D11 纹理绘制管线。
3. 完成窗口生命周期全链路：创建、显示、消息循环、关闭回收。
4. 完成 UI 核心稳定特性：脏标记、焦点切换、属性更新一致性。
5. 完成基础布局（Grid/Stack）在 DPI 变化下的行为一致性。
6. 完成控件层基础控件（Button/Panel/TextBlock/Image/Card）交互态闭环。
7. 完成动画系统核心能力（时间线、缓动、并发动画、完成回调）。
8. 完成 GPU 资源管理与设备丢失恢复主流程。
9. 补齐单元测试到关键路径全覆盖（渲染、窗口、UI、控件、动画、恢复）。
10. 提供可长期运行 demo，支持窗口交互与关闭退出。

## P1 产品化能力（建议下一阶段）

1. 引入样式表与主题切换（浅色/深色/品牌主题）。
2. 增加数据绑定接口（单向/双向）与状态同步。
3. 增加更多控件：TextBox、ListView、ScrollViewer、CheckBox、Slider。
4. 增加输入系统：双击、拖拽、键盘导航与焦点环。
5. 增加性能观测：帧耗时分位数、提交频率、资源峰值。
6. 增加日志分级和可导出诊断报告。
7. 增加错误码体系和统一异常边界。
8. 增加配置文件（JSON）驱动窗口、主题和渲染参数。

状态：已完成（首版）

## P2 工程与交付（发布前）

1. 完善 CI：x64/x86 Debug/Release 构建 + 测试 + 静态分析。
2. 增加 clang-tidy 与 clang-format 自动检查。
3. 引入集成测试（窗口消息 + 渲染提交 + 动画联动）。
4. 完成 API 文档与示例文档。
5. 完成版本号策略和变更日志（CHANGELOG）。
6. 增加安装包/发布产物（zip 或 installer）。
7. 完成兼容性矩阵（Windows 8.1/10/11，DPI 缩放场景）。

状态：已完成（首版）

## P3 后续增强（新增）

1. 接入真实 D2D + DirectWrite 绘制提交链路到纹理。
2. 增加更完整的数据绑定（双向绑定、校验器、转换器）。
3. 增加虚拟化列表和可复用 item 模板。
4. 增加输入手势（滚轮缩放、多键组合）。
5. 增加性能回放工具（trace 回放 + 抖动分析）。
6. 增加崩溃恢复和会话恢复（窗口与布局快照）。
7. 发布 MSI 安装器与签名流程。

## 完成标准

1. 所有 P0 项完成并通过回归测试。
2. `ctest` 在 x64/x86 Debug 下稳定全绿。
3. demo 可持续运行，关闭窗口后正常退出。
4. 文档、任务、测试与实现保持一致。
