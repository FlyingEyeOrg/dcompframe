# 后续完善 Todo（持续迭代）

## 当前进展（2026-04-09）

- [x] 架构演进：完成可插拔后端基础抽象（DX11/WARP + DX12 预留）。
- [x] 架构演进：完成渲染线程与 UI 线程解耦基础设施（start/stop + 线程安全队列）。
- [x] 架构演进：完成命令缓冲与批量 drain 基础能力。
- [x] 控件与交互：TextBox 增加输入法组合提交与文本选择。
- [x] 控件与交互：ListView 增加分组与虚拟可视范围计算。
- [x] 控件与交互：ScrollViewer 增加惯性滚动基础能力。
- [x] 控件与交互：增加快捷键路由与命令分发。
- [x] 可靠性：新增 soak baseline、资源峰值巡检与故障注入测试。
- [ ] 其余高阶事项持续进行中。

## 架构演进

1. 把渲染后端抽象为可插拔接口（DX11/DX12/WARP）。
2. 增加渲染线程与 UI 线程解耦。
3. 增加命令缓冲和批量提交优化。

## 控件与交互

1. 实现 TextBox 输入法支持与文本选择。
2. 实现 ListView 虚拟滚动与分组。
3. 实现 ScrollViewer 惯性滚动。
4. 增加快捷键路由与命令系统。

## 可靠性

1. 建立 soak test（长时间运行 8h+）。
2. 建立资源泄露巡检（峰值快照对比）。
3. 建立故障注入测试（设备丢失、文件损坏、配置缺失）。

## 交付与生态

1. 增加 NuGet/vcpkg 二进制分发脚本。
2. 增加 API 版本兼容策略。
3. 建立性能基线看板与回归告警。
